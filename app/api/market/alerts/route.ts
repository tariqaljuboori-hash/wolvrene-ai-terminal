import { NextRequest, NextResponse } from 'next/server';import { formatRadarAlert } from '@/lib/market/alerts/alertFormatter';import { sendDiscordAlert } from '@/lib/market/alerts/discord';import { sendTelegramAlert } from '@/lib/market/alerts/telegram';
export async function POST(req:NextRequest){const body=await req.json();const state=body?.state;if(!['REACTION_CONFIRMED','TRADE_ALLOWED','DATA_UNAVAILABLE'].includes(state))return NextResponse.json({sent:false,message:'Alert filtered by rules'});const msg=formatRadarAlert(body);const [d,t]=await Promise.all([sendDiscordAlert(msg),sendTelegramAlert(msg)]);return NextResponse.json({sent:d.status==='sent'||t.status==='sent',discord:d.status,telegram:t.status});}
